## Candidate E2E (self-service)
Status: [x]
What To do: Browse → start → chat → code → tests → checkpoint → submit → analysis
Signals to check: State sync, sandbox, chat, scoring
Bugs: """
- Time in timer is not updating, the time is changed only if there is a rerender
- Tests passed is empty in the analysis page
"""
Strategy: """
1. Timer not updating:
   Root cause: `getFormatted` is a method on the store (not reactive state). In `page.tsx:62-65` we call `const getFormatted = useTimerStore(s => s.getFormatted)` then `timerFormatted = getFormatted()` — this only re-evaluates when the component re-renders, but `seconds` changing inside the store does NOT trigger a re-render because the component subscribes to the `getFormatted` function reference (which never changes), not to `seconds`.
   Fix: Subscribe to `seconds` directly and compute the formatted string reactively. Either:
   (a) Subscribe to `seconds` and `limitSeconds` in the component/TopBar via selectors, then format inline or via a useMemo, OR
   (b) Create a `useFormattedTimer()` hook that subscribes to `seconds` + `limitSeconds` and returns the derived formatted string.
   Files: `src/app/questions/[id]/page.tsx`, `src/stores/timerStore.ts`

2. Tests passed empty on analysis page:
   Root cause: In `handleEndQuestion` (`page.tsx:146-154`), `saveSession()` is called on line 150 and `setTestSummary()` on line 151. But `saveSession` persists to sessionStorage via Zustand persist middleware. The `setTestSummary` call happens right after, but the navigation on line 152 (`router.push`) fires immediately — the test summary may not have been persisted before the page navigates away.
   Fix: include `testSummary` as a parameter of `saveSession` so both are persisted in the same `set()` call.
   Files: `src/app/questions/[id]/page.tsx`, `src/stores/questionSessionStore.ts`
"""

---

## Candidate E2E (invite)
Status: [x]
What To do: Open invite → consent → auto-start → complete → analysis
Signals to check: Invite resolution, routing, submission link
Bugs: """
- On end questions should show thank you page, and generate a submission result to the recruiter
"""
Strategy: """
1. Thank you page after submission (invite flow):
   Root cause: In `handleEndQuestion` (`page.tsx:152`), both self-service and invite flows redirect to `/questions/${id}/analysis`. For the invite flow, the candidate should instead see a "Thank you" page, and the recruiter should receive a submission result.
   Fix:
   (a) Create a new page at `src/app/invite/[code]/thank-you/page.tsx` (or `src/app/questions/[id]/submitted/page.tsx`) that shows a confirmation message ("Thank you for completing the assessment", candidate name, etc.).
   (b) In `handleEndQuestion`, check if `inviteCode` exists. If so, redirect to the thank-you page instead of the analysis page.
   (c) In `useInviteEndQuestion.ts`, after marking the submission as 'submitted', also persist the analysis results (session data, test summary, scores) to the submission record in `recruiterSubmissionsStore` so the recruiter can view them from their dashboard. This means extending `updateSubmissionStatus` (or adding a new action like `finalizeSubmission`) to accept analysis payload.
   Files: new `thank-you/page.tsx`, `src/app/questions/[id]/page.tsx`, `src/hooks/question-page/useInviteEndQuestion.ts`, `src/stores/recruiterSubmissionsStore.ts`
"""

---

## Assessment runtime integrity
Status: [x]
What To do: Timer, checkpoints, revert, tests
Signals to check: Persistence, timer bugs, state restore
Bugs: """
- Disable the end question button if no chat history
- Revert label is stuck to the bottom, and not in the order
"""
Strategy: """
1. Disable end question button when no chat history:
   Root cause: `QuestionTopBar.tsx:93` only checks `hasStarted` to show the button — there's no check for whether the candidate has actually sent any messages.
   Fix: Pass `messages.length` (or a `hasChatHistory` boolean) as a prop to `QuestionTopBar`. Disable the "End Question" button (not hide it) when `messages.length === 0`. Update the `QuestionTopBarProps` interface to include `hasChatHistory: boolean` and apply `disabled={!hasChatHistory}` on the `AlertDialogTrigger` button.
   Files: `src/components/question/QuestionTopBar.tsx`, `src/app/questions/[id]/page.tsx`

2. Revert label stuck to the bottom, not in order:
   Root cause: In `ChatPanel.tsx:198-220`, snapshots are split into AI-attached (rendered inline after their message via `afterMessageId`) and standalone (reverts/manual edits). Standalone snapshots are all rendered AFTER the last message (`ChatPanel.tsx:286-294`), regardless of when they occurred chronologically. Revert snapshots (kind='snapshot-revert') have `afterMessageId: null` so they always land in the standalone bucket at the bottom.
   Fix: Revert snapshots should be interleaved with messages by timestamp. Either:
   (a) Give revert snapshots an `afterMessageId` pointing to the message they were created after (i.e. the last message at time of revert), so they render inline, OR
   (b) Change the rendering logic to merge ALL items (messages + all snapshots) into a single timeline sorted by timestamp, instead of the current two-bucket approach. This is more robust but a bigger refactor.
   Files: `src/hooks/question/useCheckpoints.ts` (set `afterMessageId` on revert snapshots), `src/components/question/ChatPanel.tsx`
"""

---

## Error handling
Status: [x]
What To do: Break flows (invalid invite, API fail, sandbox fail)
Signals to check: No crashes, proper errors
Bugs: """
- Even if invite code is wrong, it shows the assignment — should show "invalid invite code" instead
"""
Strategy: """
1. Invalid invite code still shows the assignment:
   Root cause: The invite page (`src/app/invite/[code]/page.tsx:35-48`) does have an error state that shows "Invalid Invite Link". The issue is likely in `useInviteResolution.ts` — the React Query hook may not be correctly setting `isError` when the API returns 404/403, OR the API route at `src/app/api/invite/[code]/route.ts` is not returning an error status for invalid codes (the `resolveInviteCode` function in `src/server/question-resolver.ts` may be returning a fallback/default question instead of an error).
   Fix: Debug the chain:
   (a) Verify `resolveInviteCode()` in `question-resolver.ts` returns an error result (not a fallback) when the invite code doesn't match any candidate.
   (b) Verify the API route returns a non-2xx status (404/403) for unresolved codes — check that the response status is actually propagated.
   (c) Verify `useInviteResolution` treats non-2xx responses as errors (React Query's `queryFn` must throw on non-ok responses for `isError` to be set).
   Files: `src/server/question-resolver.ts`, `src/app/api/invite/[code]/route.ts`, `src/hooks/invite-page/useInviteResolution.ts`
"""

---

## Recruiter edits role
Status: [x]
What To do: Edit fields → save
Signals to check: Data consistency
Bugs: """
- Clicking edit role doesn't do anything
"""
Strategy: """
1. Edit Role button is a no-op:
   Root cause: In `src/app/recruiters/roles/[roleId]/page.tsx:132`, the "Edit Role" button is a plain `<Button variant="outline">Edit Role</Button>` with no `onClick` handler or link — it's a placeholder.
   Fix: Two options:
   (a) Inline editing: Add an `onClick` that toggles an edit mode state. In edit mode, swap the role title / description / status into editable fields (reuse `RoleForm` component from `src/components/recruiters/RoleForm.tsx`). On save, call `updateRole()` from `recruiterRolesStore`. Add a cancel button to exit edit mode.
   (b) Separate edit page: Create `src/app/recruiters/roles/[roleId]/edit/page.tsx` that renders `RoleForm` pre-populated with the role data, and change the button to `<Button asChild><Link href={.../edit}>Edit Role</Link></Button>`.
   Option (a) is simpler and avoids a new page. The `RoleForm` component and `useRoleForm` hook already exist and handle validation.
   Files: `src/app/recruiters/roles/[roleId]/page.tsx`, `src/components/recruiters/RoleForm.tsx`, `src/stores/recruiterRolesStore.ts`
"""

---

## Recruiter reviews submission
Status: [x]
What To do: Open → view analysis → navigate
Signals to check: Data mapping, UI correctness
Bugs: """
- Missing the functionality to change the status of a submission
"""
Strategy: """
1. No UI to change submission status:
   Root cause: The submission detail page (`src/app/recruiters/submissions/[submissionId]/page.tsx:94-96`) shows a `Badge` with the current status, but there is no dropdown/button to change it. The store action `updateSubmissionStatus` in `recruiterSubmissionsStore.ts:78-90` already exists but is never called from this page.
   Fix:
   (a) Replace the static `Badge` in the submission detail header with a `Select` (or `DropdownMenu`) that lists the valid status transitions (e.g. submitted → reviewed, in_progress → submitted, etc.).
   (b) On change, call `updateSubmissionStatus(submissionId, newStatus)` from the store.
   (c) The page is currently a server component (`async function`). To use the store + interactive select, either convert it to a client component, or extract a `SubmissionStatusSelect` client component that handles the interaction.
   (d) Consider restricting valid transitions (e.g. can't go from 'reviewed' back to 'not_started') in the UI by filtering the options.
   Files: `src/app/recruiters/submissions/[submissionId]/page.tsx`, `src/stores/recruiterSubmissionsStore.ts`, new component `src/components/recruiters/SubmissionStatusSelect.tsx`
"""

---

## Question filtering
Status: [x]
What To do: Filter by role/difficulty
Signals to check: Correct subset, UI sync
Bugs: """
- Filter status should be in the url (nuqs)
"""
Strategy: """
1. Question filters not synced to URL:
   Root cause: In `src/app/questions/page.tsx:16-17`, the filter state uses plain `useState` for `selectedRole` and `selectedDifficulty`. This means filters reset on page navigation and can't be shared via URL.
   Fix: Replace `useState` with `useQueryState` from `nuqs` (already installed and used via `useTabSearchParam`):
   ```
   const [selectedRole, setSelectedRole] = useQueryState("role", parseAsString.withDefault("all").withOptions({ history: "push" }));
   const [selectedDifficulty, setSelectedDifficulty] = useQueryState("difficulty", parseAsString.withDefault("all").withOptions({ history: "push" }));
   ```
   The `NuqsAdapter` is already set up in `src/app/providers.tsx`. Cast the values to `QuestionRole | "all"` and `QuestionDifficulty | "all"` as needed, or use `parseAsStringLiteral` for type safety.
   Files: `src/app/questions/page.tsx`
"""
